# Application Configuration
spring.application.name=movie-review-api
server.port=8080
server.servlet.context-path=/api/v1

# Database Configuration

# MongoDB Configuration
spring.data.mongodb.database=movie-api-db
spring.data.mongodb.uri=mongodb+srv://TheAKS:MovieAPI_2025@movieapi.kzzatu0.mongodb.net

# === Postgres (Supabase pooled) ===
spring.datasource.postgres.url=jdbc:postgresql://aws-1-ap-southeast-1.pooler.supabase.com:6543/postgres?sslmode=require
spring.datasource.postgres.username=postgres.qvmwyjeijozwdvynyixx
spring.datasource.postgres.password=SkAeht@Users@369$
spring.datasource.postgres.driver-class-name=org.postgresql.Driver

# JPA (keep as-is; optional: remove explicit dialect per log hint)
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect  # <- you can delete this

# Flyway: keep disabled until login works OR reuse the same pooled URL
spring.flyway.enabled=false
# (after app starts cleanly, enable and point Flyway to the same creds)
# spring.flyway.enabled=true
# spring.flyway.url=${spring.datasource.postgres.url}
# spring.flyway.user=${spring.datasource.postgres.username}
# spring.flyway.password=${spring.datasource.postgres.password}
# spring.flyway.locations=classpath:db/migration
# spring.flyway.baseline-on-migrate=true

# Security Configuration
jwt.secret=${JWT_SECRET:kZp4a8fJq9nUe3bVf6gHc8xLg0yR2wZ1iO5jK+sN/tA=}
jwt.expiration=${JWT_EXPIRATION:86400000}

# CORS Configuration
cors.allowed-origins=${CORS_ORIGINS:http://localhost:5173,http://localhost:5174,http://localhost:3000}
cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
cors.allowed-headers=*
cors.allow-credentials=true

# Logging Configuration
logging.level.com.theAkS.movies=DEBUG
logging.level.org.springframework.security=DEBUG
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# Actuator Configuration
management.endpoints.web.exposure.include=health,info,metrics
management.endpoint.health.show-details=when-authorized
